//Declarando a variável
int count = 10;

//Executa o laço uma vez e depois verifica a condição, se ainda for verdadeira continua repetindo
do
{
    Console.WriteLine(count);
    count--;
} while (count >= 1);